{
  "name": "training",
  "version": "0.0.49",
  "dependencies": {
    "parcel": "^2.6.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "build": "parcel build public/index.html && cp -a manifest.json dist/",
    "extension": "web-ext run -t chromium --source-dir ./dist/"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "devDependencies": {
    "typescript": "latest",
    "react-refresh": "0.10.0",
    "web-ext": "7.1.1"
  }
}
